# Rich pilot Site update — 2026-09-17

The owner-private Observatory now separates the rich-template pilot from historical generic-prompt experiments. Publication v2 contains42 reviewed queries and4,968 rows. Complete hosted HTML and snapshot readback matched their packaged SHA-256 hashes; receipt: `data/site-publication.json`. The original publication receipt remains in `data/site-publication-v1.json`.

The policy panel previously displayed the complete historical v1.1 experiment document. It now displays only the exact approved model-facing guide extracted by the same hash-checking builder used for inference:47,739UTF-8bytes, SHA-256`56b77c5efa6b4df0da9a240284a532da5b9d21e35e2450976da98a27718de3f0`. The exact shared policy and seven native questions are separate disclosures. Operator notes are excluded from the display and guide download. No prompt, gold label or historical result was changed.

Selecting a pilot case exposes its actual trusted context and assessed material, with preceding/following padding in expandable panels. Reconstruction must match the recorded submitted-request hash for every one of the48cases. Gold and observed answers remain outside the input. All seven judgments and distributions can be inspected separately. Family filtering, row selection, return to All, source inspection, desktop/narrow layouts and corrected policy disclosure were checked locally. No browser console errors were observed. Hosted owner editing was not checked.

The original pilot remains48attempts/47valid. One user-authorized supplemental call repaired the sandbox DNS failure with the exact frozen request and the same reported model, `jev-1.13.0`; all six scored judgments matched. The new view reports48completed cases separately from49total attempts, preserving the original failure and its unknown-usage reservation. Repair cost:$0.000561162. No automatic retries or broader Jev evaluation ran.

## Completed-case default

The Rich-template pilot now defaults to Completed cases: 48 valid cases and zero unresolved results. A Results view selector exposes Original attempts with the unchanged 47/48 first-run figures, DNS failure and seven unavailable judgments. The recovered row is labeled Recovered · attempt 2, and all seven replacement answers are inspectable. The supplemental accounting table retains 49 attempts. Completed-case latency excludes the replacement because its report has no timing; the short-length coverage remains 15 timed responses. No extra Jev call was made for this presentation change.

Verified locally: view switching, recovered row/native probabilities, original failure retention, completed denominators, chart layout and zero browser errors. Four projection tests verify binding, answer integrity and unchanged original queries.
