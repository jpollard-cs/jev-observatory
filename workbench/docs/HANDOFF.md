# Codex handoff · 0.5 reconstruction

## What to use

**Use this 0.5.0-rebuilt tree as a reviewable reconstruction on verified 0.4.1, not as recovered lost code.** The old 0.5 candidate remains unavailable. Do not search for it as a prerequisite to using this change set.

The original workspace is `/Users/jordan/Documents/Codex/2026-09-16/i-g`. Research source, `.env` and full live evidence remain in `outputs/jev-redteam`. This bundle has not been merged into that working tree or published.

1. Inspect local Git state and processes. Preserve uncommitted local work and existing frozen runs.
2. Compare against the 0.4.1 archive hash in `provenance/rebuild-lineage.json`; use `provenance/rebuild-changes.json` or the separate small change-set ZIP. Do not blindly overwrite newer local work.
3. Select one canonical development folder/branch. Keep old releases for their source-bound frozen plans.
4. Run the offline verification/tests and real browser acceptance below before any paid smoke or publishing.

## Implemented now

- A real guided journey: Describe → Review rules → Choose tests → Authorize run → Inspect results.
- Three setup checkpoints distinct from execution. Archived reports do not validate edited policy drafts.
- Rule tradeoff cards, attached help, progressive disclosure, primary dollar budget, optional advanced input-size limit and explicit unused headroom.
- Setup advice: one native request, 16 Choices, reviewed options only. Exact data disclosure, separate cost confirmation, unchecked diffs, explicit selected application, stale rejection and guarded undo.
- Existing session-key source selection, expiring one-use confirmation, pin/usage validation and append-only ledger extended to the distinct setup purpose.
- Source and instance separation: setup != catalog tags != coverage ranking != guardrail judgment.
- Existing evidence fix, all historical reports, original replay, galaxy/atlas and both catalogs retained.

Read `REBUILD-0.5.md` for the exact semantic and security limits. The application has not acquired autonomous policy generation, new test gold, tool execution, public sharing or live new setup evidence.

## Proof versus remaining checks

This reconstruction has fresh software validation, module-faithful bridge browser acceptance, source/data comparisons and a full 15,184-job replay using synthetic transport. Counts are in `validation/receipt.json`; prior release receipts retain their historical scope.

**Direct browser navigation to localhost was blocked here.** The API-only bridge preserves each real ES module but does not validate browser-to-loopback transport/CSP. Run the default non-bridge browser suite on the Mac. Keep real credentials out of that test; use the bundled explicitly synthetic test server.

Live Jev acceptance and suggestion quality are unmeasured. After local checks, propose a single setup smoke with exact description, model, call count and $0.01 stage limit for separate owner approval. This handoff does not authorize it. Existing historical model aliases/rates must not be silently rewritten.

## Current convenience limitations

- Browser drafts/review acknowledgements persist, but a saved plan screen and setup-review transaction do not automatically reconnect after full reload. Plan/source files remain immutable on disk. Better saved-plan discovery is future work.
- Only reviewed settings and three representative task starters can be suggested. Unmatched apps/languages route to manual review.
- Surface/capability suggestions affect coverage requirements, not privileges. Missing adapters stay visible.
- Current tailored catalog remains 60 cases; original matrix+extensions remains a separate 15,184-cell replay, not all available in Jev-ranked admission selection.
- A session key pasted into the server does not propagate to independent CLI runs; those continue using their project/account environment.

## Acceptance commands

```bash
npm run verify
npm test
node --test vendor/admission-v1/vendor/compiler/tests/compiler.test.mjs
node scripts/audit-rebuild.mjs --baseline /absolute/path/to/unmodified-0.4.1
node scripts/validate-replay-scale.mjs
```

For an actual browser test, use the synthetic server as documented in `ACCEPTANCE-0.5.md`. It runs the production routes with an injected fake transport and rejects real keys; do not add a production bypass route for it.

## Do not regress

Preserve original labels, request bodies, missing/unasked-field handling and complete observation identity. Keep all $3 research-account history and unresolved holds until the owner separately changes authorization. Never treat larger planner values as account authorization. Do not flatten browser ES modules for tests. Preserve full-height navigation, default-on galaxy, reduced motion and interactive data visualizations. Do not promote imported source claims or model suggestions to authority.

The latest uploaded live evidence still includes the completed 480-call admission experiment. This reconstruction performed zero real provider calls and does not improve its reported results retroactively.

## Code map

- `src/setup.mjs`: fixed choices, proposals, allowlisted application and guarded undo.
- `src/selection/advisor.mjs`: setup request planning, purpose separation, validation and summaries.
- `src/local-connection.mjs`, `server.mjs`: existing key/paid boundary plus setup routes.
- `public/workflow-model.js`, `guided.js`, `guided.css`, `app.js`: progression and review surfaces.
- `tests/setup.test.mjs`: 26 new unit/integration/synthetic transport tests.
- `scripts/ui-browser/acceptance05.py`, `mock-setup-server.mjs`: reviewable browser acceptance.
- `scripts/audit-rebuild.mjs`: exact source/payload/evidence comparison.

Earlier source archives remain in the inherited provenance chain. `docs/archive/` preserves the overwritten 0.4.1 entry documents. The small change set is a manual merge aid, not an installer. Nothing should copy these modules into frozen historical run directories.
