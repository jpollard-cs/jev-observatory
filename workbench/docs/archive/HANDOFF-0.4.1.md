# Maintainer / Codex handoff · 0.4.1

## Current changes

Read `LOCAL-CONNECTION-AND-FIXES.md`. Repaired Evidence explorer's undefined `fields`/`judgment` bindings and recursive error rendering. The previous browser helper flattened modules; the current helper preserves ES-module scopes and exposed the real failure. Budget validation is now action-scoped, inactive settings are normalized where appropriate, and positive safe planning amounts can exceed the old experimental ceilings. Preview saturation is not permission to spend.

A new single-user local connection keeps a pasted key in the Node process only, or reads only the key from an explicitly selected `.env`/process environment. Saved ranking/tagging plans may run from the browser after a one-use, expiring, plan/key-bound confirmation. No general model proxy or evaluation/replay web-execution route exists. The underlying lock, ledger, settlement and no-retry execution are retained. Account initialization is explicit; existing project usage and $3 authorization stay intact. Existing account limits are not changed by this form.

## Preservation

`provenance/parent-workbench-0.4.zip` retains the unmodified parent and its historical chain. Repeated older full ZIP copies at this level may be omitted to avoid exponential release size; they remain inside the parent. Historical reports/data, all vendored original builders and current-policy request bodies remain unchanged. Source comparisons and report bindings are in `validation/source-bindings-0.4.1.json`.

Keep the user's original Git tree, complete `runs/`, original source archive, `.env`, and local ledger. They are outside this distribution. Original path: `/Users/jordan/Documents/Codex/2026-09-16/i-g/outputs/jev-redteam`.

The executable policy-tailored catalog remains 60 cases. The separate historical suite still supports 15,120 original matrix cells plus 64 original boundary cases. We did not alter their policies, expectations, or available coverage.

## Resume / operate

- Start the new app on port 8791, not the old 8790 tab. No inference happens on startup.
- Use matching old sources to resume old frozen plans; do not bypass source-hash mismatches.
- Pasted session credentials are only available to the local server's advisor execution. Separate evaluation/replay CLI processes use their existing documented credential source. They cannot magically inherit a server's private memory.
- Selecting a new key does not reset the account. Loading a key is not provider verification. Cached runs do not verify new credentials.
- Account totals and budgets are local bookkeeping, not a provider invoice balance. Never initialize another ledger to bypass unresolved charges.
- Changes to settings invalidate saved preview identities; revised schemas distinguish inactive allowlists and optional token constraints.
- All user-facing errors should preserve navigation and the selected evidence identity, never recursively re-enter a failing renderer.
- Do not confuse raw metadata advice with target-model evaluation results. Tagging remains a proposal; neither stage generates unreviewed gold.

## Verification boundary

`validation/receipt.json` names current software, HTTP, module-faithful browser and simulated execution checks. Direct browser navigation to localhost is blocked in this build environment. Browser checks bridge only API transport, while actual modules retain lexical scope; HTTP routes/security are also tested directly. Complete direct browser smoke testing on the Mac remains appropriate.

No real TypeSafe calls, production deployment, original-project edits or live ledger writes were made by this development session. The test transport/key fixtures are synthetic and are not included in dashboards as measured evidence. Masked HTTP-error bodies are deliberately not byte-identical provider responses. No OS Keychain persistence or multitenant hosting is implemented.
