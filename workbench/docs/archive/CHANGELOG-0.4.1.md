# 0.4.1 — local connection and evidence repair

- Fix unbound Evidence explorer variables and recursive renderer error handling.
- Replace flattened-module browser QA with faithful separate ES modules.
- Accept larger positive planning budgets; show complete coverage, selected cost and unused allowance. Preserve independent account authorization.
- Validate only active-action inputs; allow no extra input-size planning cap.
- Add local process-memory credentials and explicit `.env` key extraction.
- Permit only saved, confirmed advisor metadata requests in the browser; one-use expiring approvals, fixed destination, no automatic retries, same account ledger.
- Canonicalize advisory caches while retaining raw imported JSON independently.
- Existing observations, labels, policy request bodies, catalog and atlas remain unchanged.

## Previous changes (historical)

# 0.4.0 · Outcome atlas and original evaluation suites

- Rebuilt outcome atlas as an interactive perspective Canvas scene: family/outcome/real-metric layouts, smooth orbit and camera focus, anomaly navigation, exact observation drilldown, local image export, keyboard controls and reduced-motion support.
- Added executable original replay planner for all 15,120 original matrix cells plus 64 integrity/judge/moderation/scope extension cases. Predefined sweeps, factor filters, indivisible contrasts, explicit budgets and new run identities.
- Preserved original typed questions and original annotations; grade only requested judgments. Did not map historical policies into current admission presets.
- Added scalable incremental request persistence, small per-response progress checkpoints and periodic compact reports. Differentially tested ledger optimization retains the exact original budget reducer semantics.
- Added the original campaign's 3,920 planned rows and 32-observation encoding diagnostic to the recorded evidence library. Missing/unknown attempts remain visible; no simulated outcomes are displayed.
- Added original-question-aware evidence tables, native Boolean scoring, matched original source inspection and explicit no-decision-asked states.
- Kept the existing 60-case custom-policy/advisor planner distinct. Its tested payloads and expectations remain unchanged.
- Restored label-adjacent explanation popovers and plain-language budget/audit labels from the user's earlier UI feedback. Existing full-height navigation and original galaxy background retained.
- No provider calls, original account mutations, production effects or publication performed during implementation.
