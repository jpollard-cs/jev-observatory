import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {projectAdmissionEvidence} from '../integrations/observatory/project-evidence.mjs';
test('Observatory projection preserves source identity and separate policy conditions',()=>{const r=projectAdmissionEvidence(fs.readFileSync(new URL('../data/consumer-admission-v1.report.json',import.meta.url),'utf8'));assert.equal(r.recordedCalls,480);assert.equal(r.conditions.length,8);assert.equal(r.conditions.find(c=>c.id==='strict_criteria').contract.correct,64);assert.match(r.sourceHash,/^[a-f0-9]{64}$/);});
