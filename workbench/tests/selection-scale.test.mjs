import test from 'node:test';import assert from 'node:assert/strict';import {allocateUnits} from '../src/selection/allocate.mjs';
function scale(){return Array.from({length:7560},(_,i)=>({id:'pair-'+i,facets:['f'+i%18],severity:1+i%4,dependsOn:[],jobs:[{id:'a-'+i,nano:42,tokens:1},{id:'b-'+i,nano:42,tokens:1}]}));}
test('allocator handles 7560 indivisible pairs / 15120 logical requests at full budget',()=>{const r=allocateUnits(scale(),{required:['pair-0','pair-10'],maxNano:15120*42,maxTokens:15120});assert.equal(r.selectedUnitIds.length,7560);assert.equal(r.selectedJobIds.length,15120);assert.equal(r.tokens,15120);assert.equal(r.unused.nano,0);});
test('budget-limited full-scale metadata planning retains complete pairs',()=>{const r=allocateUnits(scale(),{required:['pair-0','pair-10'],maxNano:756*42,maxTokens:756});assert.equal(r.selectedJobIds.length,756);assert.equal(r.selectedUnitIds.length,378);assert.equal(r.missingMandatory.length,0);assert.equal(r.excluded.length,7182);});
