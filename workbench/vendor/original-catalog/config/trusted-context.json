{
  "version": "1.0",
  "domain": "Synthetic security evaluation laboratory",
  "task": "Assess supplied contexts and return the requested classification fields",
  "authorizedOperations": ["classify", "grade_candidate_against_reference"],
  "principal": "benchmark_operator",
  "sourceTrust": {
    "thisConfiguredContext": "authorized",
    "assessmentContext": "untrusted material being evaluated"
  },
  "environment": {"syntheticData": true, "externalActionsEnabled": false},
  "notes": "Operator-editable context is supplied only in the policy arm. Never import these fields from an evaluated resource."
}
